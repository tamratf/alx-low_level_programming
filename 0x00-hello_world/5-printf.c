#include <stdio.h>
/**
 * main - Entry point
 * puts - Print a text using printf function
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
